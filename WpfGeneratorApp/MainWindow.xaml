<Window x:Class="WpfCodeGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WPF Code Generator" Height="550" Width="600"
        Background="#F9F9F9">
    <Window.Resources>
        <!-- Style for Labels -->
        <Style TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <!-- Style for TextBoxes -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Style for Buttons -->
        <Style TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/> <!-- Ensure full text is visible -->
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="BorderBrush" Value="#005A9E"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#005A9E"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
        <StackPanel>
            <!-- Project Name -->
            <TextBlock Text="Project Name:"/>
            <TextBox x:Name="ProjectNameTextBox"/>

            <!-- Main Window Name -->
            <TextBlock Text="Main Window Name:"/>
            <TextBox x:Name="MainWindowNameTextBox"/>

            <!-- Main Window Title -->
            <TextBlock Text="Main Window Title:"/>
            <TextBox x:Name="MainWindowTitleTextBox"/>

            <!-- Root Namespace -->
            <TextBlock Text="Root Namespace:"/>
            <TextBox x:Name="RootNamespaceTextBox"/>

            <!-- Output Folder -->
            <TextBlock Text="Output Folder:"/>
            <DockPanel Margin="0,0,0,20">
                <TextBox x:Name="OutputFolderTextBox" Width="455" Margin="0,0,5,0"/> <!-- Make wide enough to use all available space -->
                <Button Content="Browse..." Click="BrowseOutputFolder_Click" Width="80" HorizontalAlignment="Right"/>
            </DockPanel>

            <!-- Generate Button -->
            <Button Content="Generate Project" HorizontalAlignment="Center" Click="GenerateProject_Click"/>
        </StackPanel>
    </Grid>
</Window>