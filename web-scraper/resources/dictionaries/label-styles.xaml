<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--....................................................................-->
    <Style x:Key="LabelStyle04" TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="45"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Setter.Value>
        </Setter>
    </Style>
    <!--....................................................................-->
    <Style x:Key="LabelStyleTest01" TargetType="{x:Type Label}">
        <Setter Property="Foreground"                 Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="Background"                 Value="Transparent"/>
        <Setter Property="Padding"                    Value="5"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment"   Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" 
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- ...........................................................-->
    <Style x:Key="LabelStyle01" x:Name="LabelStyle01" TargetType="{x:Type Label}">
        <Setter Property="Background" Value="Red"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="0">
                <Setter Property="Background" Value="Red"/>
                <Setter Property="Foreground" Value="Black"/>
            </Trigger>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="Green"/>
                <Setter Property="Foreground" Value="Coral"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    <!-- ...........................................................-->
    <Style x:Key="LabelStyle03" TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Top"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!-- ...........................................................-->
    <Style x:Key="LabelStyleToolbarTitle" TargetType="{x:Type Label}">
        <Setter Property="Background"           Value="Transparent"/>
        <Setter Property="Foreground"           Value="Black"/>
        <Setter Property="FontSize"             Value="12"/>
        <Setter Property="Padding"              Value="0,0,0,0"/>
        <Setter Property="Margin"               Value="0,-1,0,0"/>
        <Setter Property="HorizontalAlignment"  Value="Center"/>
        <Setter Property="HorizontalContentAlignment"  Value="Center"/>
    </Style>
    <!-- ...................................................................-->

    <!-- ...................................................................-->
    <Style x:Key="LabelStyleToggle" TargetType="{x:Type Label}">
        <!--................................................................-->
        <Setter Property="Background"          Value="Transparent" />
        <Setter Property="Foreground"          Value="CornflowerBlue"          />
        <Setter Property="BorderThickness"     Value="0.5"             />
        <Setter Property="FontSize"            Value="21.5"              />
        <Setter Property="FontWeight"          Value="Regular"              />
        <Setter Property="FontFamily"          Value="Arial"              />
        <Setter Property="Margin"              Value="0,-1,0,4"        />
        <Setter Property="Padding"             Value="0,0,0,0"         />
        <!--................................................................-->
        <Style.Triggers>
            <!--............................................................-->
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#0264ad"/>
                <Setter Property="Background" Value="#e5f1f7"/>
            </Trigger>
            <!--............................................................-->
        </Style.Triggers>
        <!--................................................................-->
    </Style>
    <!-- ...................................................................-->
    <!--#region Begin Region LabelRoundCorner                               -->
    <!--....................................................................-->
    <Style x:Key="LabelRoundCorner" TargetType="{x:Type Label}">
        <Setter Property="Template">
            <!--............................................................-->
            <Setter.Value>
                <!--........................................................-->
                <ControlTemplate TargetType="{x:Type Label}">
                    <!--....................................................-->
                    <Viewbox>
                        <!--................................................-->
                        <Border CornerRadius="5" Background="Transparent" Width="125" Height="125" BorderBrush="LightSteelBlue" BorderThickness="2" Margin="2">
                            <!--............................................-->
                        </Border>
                        <!--................................................-->
                    </Viewbox>
                    <!--....................................................-->
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    <!--....................................................................-->
    <!--#endregion End Region LabelRoundCorner                              -->
    <!--....................................................................-->
</ResourceDictionary>
    