<Window x:Class="PasswordGenerator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PasswordGenerator.ViewModels"
        mc:Ignorable="d"
        Title="Password Generator" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Number of passphrases:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Text="{Binding PassphraseCount, UpdateSourceTrigger=PropertyChanged}" 
                     Width="50" VerticalAlignment="Center"/>
            <Button Content="Generate" Command="{Binding GenerateCommand}" 
                    Margin="10,0,0,0" Padding="10,5"/>
        </StackPanel>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <TextBox Text="{Binding Passphrase}" 
                     TextWrapping="Wrap" 
                     AcceptsReturn="True" 
                     IsReadOnly="True" 
                     FontFamily="Consolas" 
                     VerticalScrollBarVisibility="Auto" 
                     HorizontalScrollBarVisibility="Disabled"
                     />
        </ScrollViewer>
    </Grid>
</Window>
    